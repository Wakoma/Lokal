version: "3.8"
services:
  moodledb:
    image: docker.io/bitnami/mariadb:10.5
    environment:
      - ALLOW_EMPTY_PASSWORD=yes
      - MARIADB_USER=bn_moodle
      - MARIADB_DATABASE=bitnami_moodle
      - MARIADB_CHARACTER_SET=utf8mb4
      - MARIADB_COLLATE=utf8mb4_unicode_ci
    volumes:
      - '{{project_root}}/moodle/db:/bitnami/mariadb'
    networks:
      - moodle
    # we don't actually need to expose the port because nobody was 
    # ports:
    #  - 3307:3306

  moodle:
    image: docker.io/bitnami/moodle:3
    environment:
      - MOODLE_DATABASE_HOST=moodledb
      - MOODLE_DATABASE_PORT_NUMBER=3306
      - MOODLE_DATABASE_USER=bn_moodle
      - MOODLE_DATABASE_NAME=bitnami_moodle
      - ALLOW_EMPTY_PASSWORD=yes
    volumes:
      - '{{project_root}}/moodle/app:/bitnami/moodle'
      - '{{project_root}}/moodle/data:/bitnami/moodledata'
    depends_on:
      - moodledb
    labels:
      traefik.enable: "true"
      traefik.http.routers.moodle.entrypoints: websecure
      traefik.http.routers.moodle.rule: Host(`moodle.{{domain}}`)
      traefik.http.routers.moodle.tls: "true"
{% if server_is_live %}
      traefik.http.routers.moodle.tls.certresolver: {{cert_resolver}}
{% endif %}
      traefik.http.services.moodle.loadbalancer.server.port: 80
    networks:
      - moodle
      - traefik

networks:
  traefik:
    external: true
  moodle:
    name: moodle

test:
  service: Disk
  root: <%= Rails.root.join('tmp/storage') %>

local:
  service: Disk
  root: <%= Rails.root.join('storage') %>

s3:
  service: S3
  access_key_id: "{{qpixel_minio_user}}"
  secret_access_key: "{{qpixel_minio_password}}"
  region: local
  bucket: "{{qpixel_minio_bucket}}"

test:
  service: Disk
  root: <%= Rails.root.join('tmp/storage') %>

local:
  service: Disk
  root: <%= Rails.root.join('storage') %>

s3:
  service: S3
  access_key_id: "{{app_bucket_user}}"
  secret_access_key: "{{app_bucket_password}}"
  region: local
  bucket: "{{app_bucket_name}}"

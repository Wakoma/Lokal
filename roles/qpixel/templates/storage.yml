test:
  service: Disk
  root: <%= Rails.root.join('tmp/storage') %>

local:
  service: Disk
  root: <%= Rails.root.join('storage') %>

s3:
  service: S3
  access_key_id: "qpixel"
  secret_access_key: "qpixel"
  region: local
  bucket: "qpixel"

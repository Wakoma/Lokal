{
    "version": "10",
    "aliases": {
        "local": {
                "url": "http://localhost:9000",
                "accessKey": "{{admin_user}}",
                "secretKey": "{{admin_password}}",
                "api": "S3v4",
                "path": "auto"
        }
    }
}
# Wordpress

Favourite web CMS of all. Define `wordpress_domain` or `wordpress_subdomain`
to configure the final domain name.

You will get instance already setup with `admin_username` and `admin_password`.
# Mail

We use Maddy as the email server.

## Users

In case you have `users` role then it will take users dynamically created by the admin.
Otherwise, you would need to SSH to the server and do some docker/bash magic as shown
at https://maddy.email/tutorials/setting-up/ (at the bottom).
all:
  hosts: "localhost"
  vars:
    server_is_live: false
    debug: true
    lokal_secret: verysecret
    password_admin: verypassword
    domain: lokal.localhost
    project_root: "$HOME/lokal"
    backup_root: "$HOME/lokal-backup"
    ansible_connection: local
    services:
    - base
    # - mail
    # - auth
    - wordpress
